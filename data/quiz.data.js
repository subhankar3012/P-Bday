/* ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   QUIZ.DATA.JS — Favorite Color Quiz Data Content
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ */

import { APP_CONFIG } from '../config/app.config.js';

export const COLOR_QUIZ_DATA = {
  question: "What is Priyanka's favorite color?",
  subtitle: 'Pick a bubble to transform the whole app! ✨',
  options: APP_CONFIG.themes
};
